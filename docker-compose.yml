services:
  mcp-server:
    container_name: mcp-server 
    build: .
    stdin_open: true   # -i
    tty: true          # -t
    env_file:
      - .env
    ports:
      - "3007:3007"
    restart: unless-stopped